# Backend API and WebSocket URL. Required when deploying (e.g. Railway).
# Set this to your backend's public URL so the frontend can proxy API calls and connect Socket.IO.
# Example for Railway: https://your-backend-service.up.railway.app
BACKEND_URL=http://localhost:3001
